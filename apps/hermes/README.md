# Hermes v2 placeholder

Hermes v1 source is unavailable. This package intentionally contains no Telegram gateway, provider credentials, database access, or reconstructed v1 logic.

Phase 1 only reserves the application boundary. The clean Hermes implementation is planned for Phase 6 and will use a scoped HolyMedia API/MCP service token with read-only defaults.
